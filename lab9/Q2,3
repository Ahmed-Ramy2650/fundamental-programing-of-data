#this function is called it self ifinit numbers of times
#the fix code is -->

def sumVal(N):
    if N <= 0:
        return 0
    return N + sumVal(N - 1)

print(sumVal(4))
#-----------------------------------------
#Q3 


#the output is -->
12
3

#this function finding the greatest common divisor
